# ============================================
# Art Pieces App - Environment Variables
# ============================================
# Copy this file to .env.local and fill in your actual values
# This file serves as a template for developers
#
# To get started:
# 1. Copy this file: cp .env.example .env.local
# 2. Fill in your actual values below
# 3. Never commit .env.local to git (already in .gitignore)

# ============================================
# Contact Information (Displayed in Footer)
# ============================================
# Contact email configuration:
#   - Set to "email:myemail@example.com" to show email in footer
#   - Set to "redirect:/path/to/page" to redirect email icon to a specific path
#   - Leave empty or unset to use default redirect path (/contact)
CONTACT_EMAIL=redirect:/contact

# Your Instagram profile URL (shown in footer)
INSTAGRAM_URL=https://instagram.com/yourusername

# ============================================
# Email Configuration (SMTP)
# ============================================
# Required: SMTP server hostname
# Examples:
#   - Gmail: smtp.gmail.com
#   - Outlook: smtp-mail.outlook.com
#   - SendGrid: smtp.sendgrid.net
#   - Mailgun: smtp.mailgun.org
SMTP_HOST=smtp.gmail.com

# Optional: SMTP port (defaults to 587 if not set)
# Common ports: 587 (TLS), 465 (SSL), 25
SMTP_PORT=587

# Optional: Use secure connection (true/false)
# Automatically set to true if port is 465
SMTP_SECURE=false

# Required: SMTP username (usually your email address)
SMTP_USER=your-email@gmail.com

# Required: SMTP password
# For Gmail, you may need to use an "App Password" instead of your regular password
# Generate one at: https://myaccount.google.com/apppasswords
SMTP_PASSWORD=your-app-password-here

# Optional: Name shown in "From" field (defaults to BRAND_NAME)
SMTP_FROM_NAME=My Art Pieces

# Optional: Email shown in "From" field (defaults to SMTP_USER)
SMTP_FROM_EMAIL=your-email@gmail.com

# ============================================
# Application Configuration
# ============================================
# Required: Email address to receive form submissions
# This is where custom painting and tutoring requests will be sent
RECIPIENT_EMAIL=your-email@gmail.com

# Optional: Brand name (defaults to "Art Pieces")
# Used in email templates and throughout the app
BRAND_NAME=My Art Pieces

# ============================================
# Next.js Configuration
# ============================================
# Optional: Next.js environment
# Set to "production" for production builds
NODE_ENV=development

# ============================================
# Public Environment Variables (Client-side)
# ============================================
# These variables are exposed to the browser (must start with NEXT_PUBLIC_)
# Contact email configuration:
#   - Set to "email:myemail@example.com" to show email in footer
#   - Set to "redirect:/path/to/page" to redirect email icon to a specific path
#   - Leave empty or unset to use default redirect path (/contact)
NEXT_PUBLIC_CONTACT_EMAIL=redirect:/contact

# Your Instagram profile URL (shown in footer)
NEXT_PUBLIC_INSTAGRAM_URL=https://instagram.com/yourusername

# ============================================
# Email Confirmation Settings
# ============================================
# Enable/disable sending thank you confirmation email to requester
# Set to "false" to disable, "true" or leave empty to enable (default: enabled)
SEND_CONFIRMATION_EMAIL=true
